# Configuração do Netlify para o Projeto Nexa Clínica Integrada

[build]
  # Pasta raiz que contém os arquivos estáticos
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
